name = "okMambaOut/moonpetri"

version = "0.1.0"

readme = "README.md"

license = "MIT"

keywords = [ "petri-net", "reachability", "pnml", "model-checking", "moonbit" ]

description = "Deterministic discrete Petri net modeling and reachability analysis for MoonBit"

preferred_target = "wasm-gc"
