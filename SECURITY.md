# 安全与资源边界

- 这是离线建模工具，不是交通、工业或并发系统的安全认证器。
- PNML 扫描器限制输入为 2,000,000 个 UTF-16 code unit、元素递归深度不超过 16；拒绝 DTD、外部实体、数字实体、扩展元素和 namespace。错误位置也是 UTF-16 偏移，不是字节偏移。
- CLI 文件层在解析前读取整个文件，因此扫描器限制不等同于磁盘读取配额。不要向 CLI 传入不可信的超大文件；宿主应用应在读取时自行限制字节数。
- `max_states` 是保存状态数的硬上限，不是运行时间或内存预算。place/transition 很多时，单个状态仍可能消耗显著资源。建议不可信模型放入有时间、内存限制的进程。
- token 和 arc 权重使用 32 位有符号整数；检查合并、产生和解析溢出。溢出返回错误，不当作死锁。
- `truncated=true` 表示探索不完整，不证明无界，也不证明其他状态没有死锁。
- 不执行 XML 中的代码，不访问外部实体、不发起网络请求。依赖下载和 CI 安装发生在构建阶段，不属于分析运行时。

漏洞报告请通过仓库 GitHub Security Advisories 的私密报告功能（若已启用）联系维护者。未启用时先提交不包含攻击数据或私人信息的 issue 请求私下联系，不公开凭据、个人数据或危险载荷。
