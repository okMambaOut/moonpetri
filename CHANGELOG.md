# 变更日志

## 0.1.0 — 2026-09-09

- 补全 MoonCakes repository 元数据；新增实际 ZIP 发布前审计和 7 项安全回归，拒绝路径穿越、符号链接、申报书、凭据和构建缓存进入发布包。

- 完成真实 PNML 子集导入、规范化导出、文件 CLI 和三个可执行场景。
- 修复权重／token 溢出、非法 marking、失败序列定位和可写快照泄漏。
- 使用线性前驱存储重建 BFS 最短轨迹，记录稳定死锁证据，明确 Unknown 有界性语义。
- 添加容量守恒／加权 self-loop 穷举、PNML 安全边界及 CLI 进程验收。
- 中文 README 与实际 API、运行命令和边界对齐；补全 MIT 和 AI／依赖来源声明。
- 经用户授权纠正原 AI 生成历史的 Git 身份；该操作不计为新增有效提交。

- 删除并重建同名公开仓库，公开提交身份与唯一贡献者 API 均核验为 okMambaOut。
- CI 固定本地验证编译器，Ubuntu 四目标构建、测试和实际 CLI 验收通过；发布前对最终提交再次执行 CI。

限制：完整 PNML 和强类型跨网 ID 不在本版实现范围。GitHub Release 与 MoonCakes 是独立发布目标，不以其一存在推断另一项已发布。
